spring:

  jpa:
    hibernate:
      ddl-auto: create-drop
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      show-sql: false
  test:
    database:
#      replace: none

logging:
  level:
    root: INFO
  file:
    name: logs/test.log
